# M25.1: a drop body cannot drop `self` — the binding is
# being destroyed by the runtime; consuming it from the body
# would corrupt the caller's drop chain.

struct Owner
  cell: *Cell(Int)

  drop self: !Owner
    -self
