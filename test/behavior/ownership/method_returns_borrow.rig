# A `?self` method may return a borrow into `self`.

struct Wrap
  payload: Int

struct Holder
  inner: Wrap

  fun get(?self) -> ?Wrap
    ?self.inner

sub main()
  h = Holder(inner: Wrap(payload: 3))
  b = h.get()
  print(b.payload)

# expect:
# 3
