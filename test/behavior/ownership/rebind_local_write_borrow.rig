# A local write borrow can be rebound to borrow another value; the first
# is free again once nothing borrows it.

struct Wrap
  n: Int

sub main()
  x = Wrap(n: 1)
  y = Wrap(n: 2)
  w = !x
  w.n = 10
  w = !y
  w.n = 20
  print(x.n, y.n)

# expect:
# 10 20
