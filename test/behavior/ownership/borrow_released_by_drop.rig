# Dropping a borrow binding ends the borrow.

struct Wrap
  payload: Int

  drop(!self)
    print("drop")

sub main()
  b = Wrap(payload: 1)
  r = ?b
  print(r.payload)
  -r
  -b
  print("end")

# expect:
# 1
# drop
# end
