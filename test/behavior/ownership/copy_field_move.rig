# Moving a Copy field out of a struct copies it; the struct stays whole.

struct Wrap
  payload: Int

  drop(!self)
    print("drop")

struct Pair
  a: *Wrap
  n: Int

sub main()
  p = Pair(a: *Wrap(payload: 1), n: 5)
  m = <p.n
  print(m + p.n)

# expect:
# 10
# drop
