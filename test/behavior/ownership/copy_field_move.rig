# Moving a Copy field out of a struct copies it; the struct stays whole.

struct Box
  payload: Int

  drop(!self)
    print("drop")

struct Pair
  a: *Box
  n: Int

sub main()
  p = Pair(a: *Box(payload: 1), n: 5)
  m = <p.n
  print(m + p.n)

# expect:
# 10
# drop
