# `_ = value` discards a value; a discarded resource is dropped at once.

struct Wrap
  n: Int

  drop(!self)
    print("drop", self.n)

fun make(n: Int) -> *Wrap
  *Wrap(n: n)

sub main()
  _ = make(1)
  rc = make(2)
  _ = <rc
  _ = 3
  print("end")

# expect:
# drop 1
# drop 2
# end
