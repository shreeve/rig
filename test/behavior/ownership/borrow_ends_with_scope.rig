# A borrow bound in an inner block ends with the block, so the owner can
# be dropped afterwards.

struct Wrap
  payload: Int

  drop(!self)
    print("drop")

sub main()
  a = Wrap(payload: 3)
  if true
    r = ?a
    print(r.payload)
  -a
  print("end")

# expect:
# 3
# drop
# end
