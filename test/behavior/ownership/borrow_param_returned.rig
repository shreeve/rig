# A function may return one of its borrowed parameters, chosen by an
# if-expression; the result borrows from both arguments.

struct Wrap
  payload: Int

fun pick(a: ?Wrap, b: ?Wrap, first: Bool) -> ?Wrap
  if first
    a
  else
    b

sub main()
  x = Wrap(payload: 1)
  y = Wrap(payload: 2)
  r = pick(?x, ?y, false)
  print(r.payload)

# expect:
# 2
