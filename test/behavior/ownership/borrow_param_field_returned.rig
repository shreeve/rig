# A borrow of a field of a borrowed parameter may be returned.

struct Wrap
  payload: Int

struct Holder
  inner: Wrap

fun inner_of(h: ?Holder) -> ?Wrap
  ?h.inner

sub main()
  h = Holder(inner: Wrap(payload: 9))
  b = inner_of(?h)
  print(b.payload)

# expect:
# 9
