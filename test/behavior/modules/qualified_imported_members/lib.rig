pub enum Shape
  circle(r: Int)
  dot

pub struct P
  x: Int
  y: Int

  fun at(x: Int, y: Int) -> P
    P(x: x, y: y)

  fun sum(?self) -> Int
    self.x + self.y

pub struct Q
  n: Int

  sub grow(q: !Q, by: Int)
    q.n += by
