# An imported function takes its compile-time arguments in brackets, as
# a local one does.

use lib

sub main()
  print(lib.scaled[3](5))
  lib.banner[40]()
  lib.banner[8]()

# expect:
# 15
# 40
# 8
