# An exhaustive statement-position match on an enum runs the matching arm.

enum Color
  red
  green
  blue

sub main()
  c: Color = .green
  match c
    .red => print "R"
    .green => print "G"
    .blue => print "B"

# expect:
# G
