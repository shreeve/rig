# `break if c` leaves the loop only when `c` holds.
# Known bug (A1): emit ignores the guard, so the loop breaks after the
# first iteration and prints only 1.

sub main()
  i = 0
  while i < 10
    i += 1
    print(i)
    break if i > 3

# expect:
# 1
# 2
# 3
# 4
