# `new rc = ...` shadows a resource that was never used; the first box is
# still dropped at the end of the scope.

struct Box
  payload: Int

sub main()
  rc = *Box(payload: 1)
  new rc = *Box(payload: 2)
  print(rc.payload)

# expect:
# 2
