# A closure can call a captured `*sub()`.

sub main()
  count: *Cell(Int) = *Cell(value: 0)
  inc: *sub() = *|+count| count.set(count.get() + 1)
  twice = |+inc|
    inc()
  twice()
  twice()
  print(count.get())

# expect:
# 2
