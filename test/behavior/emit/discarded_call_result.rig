# A call whose result is discarded is still evaluated. `<n` ends `n`,
# even for a Copy value; assigning `n` again makes it usable.

fun consume(n: Int) -> Int
  print("consumed", n)
  n

sub main()
  n = 1
  consume <n
  n = 5
  print n

# expect:
# consumed 1
# 5
