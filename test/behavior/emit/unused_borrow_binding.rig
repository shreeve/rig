# A borrow binding need not be read. Dropping the read borrow `r` ends
# it, so the write borrow `w` is allowed.

struct User
  name: String

sub main()
  user = User(name: "Steve")
  r = ?user
  -r
  w = !user
  print("ok")

# expect:
# ok
