# `lo..hi` in a match arm is an inclusive range pattern.

sub bucket(x: Int)
  match x
    1..3 => print("low")
    4..6 => print("mid")
    other => print("high")

sub main()
  bucket(3)
  bucket(4)
  bucket(6)
  bucket(9)

# expect:
# low
# mid
# mid
# high
