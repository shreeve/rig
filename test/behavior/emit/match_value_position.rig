# A match used as a binding's value; arm results unify to one type.

enum Color
  red
  green
  blue

sub main()
  c: Color = .green
  x = match c
    .red => 1
    .green => 2
    .blue => 3
  print(x)

# expect:
# 2
