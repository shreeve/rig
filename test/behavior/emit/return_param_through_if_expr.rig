# Returning an owned parameter through an if-expression moves it out:
# the caller receives a live handle.

struct Box
  n: Int

fun keep(a: *Box, flag: Bool) -> *Box
  if flag
    a
  else
    a

sub main()
  x = *Box(n: 1)
  z = keep(<x, true)
  print(z.n)

# expect:
# 1
