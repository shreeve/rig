# Returning an owned parameter through an if-expression moves it out:
# the caller receives a live handle.

struct Wrap
  n: Int

fun keep(a: *Wrap, flag: Bool) -> *Wrap
  if flag
    a
  else
    a

sub main()
  x = *Wrap(n: 1)
  z = keep(<x, true)
  print(z.n)

# expect:
# 1
