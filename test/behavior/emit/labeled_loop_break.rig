# `break :outer` inside a nested loop exits the loop labeled `:outer`,
# so the `print(99)` after the inner loop never runs.

sub main()
  :outer while true
    while true
      break :outer
    print(99)
  print(1)

# expect:
# 1
