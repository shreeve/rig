# `new rc = ...` shadows a resource binding that was already used; both
# boxes stay valid until the scope ends, where each is dropped.

struct Box
  payload: Int

sub main()
  rc = *Box(payload: 1)
  print(rc.payload)
  new rc = *Box(payload: 2)
  print(rc.payload)

# expect:
# 1
# 2
