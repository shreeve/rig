# When only literals give a type parameter its type, a float literal
# makes it `Float`, in any argument order, for generic functions and
# generic types alike.

fun max[T](a: T, b: T) -> T
  a if a > b else b

struct P[T]
  a: T
  b: T

sub main()
  print(max(3, 2.5), max(2.5, 3), max(1, 2))
  p = P(a: 3, b: 2.5)
  q = P(a: 2.5, b: 3)
  print(p.a, p.b, q.a, q.b)

# expect:
# 3.0 3.0 2
# 3.0 2.5 2.5 3.0
