# A type parameter the body never uses is still a valid parameter.

struct Tag[T]
  n: Int

enum Marker[T]
  one
  two

sub main()
  t: Tag[String] = Tag(n: 1)
  print(t.n)
  m: Marker[Int] = .two
  print(m)

# expect:
# 1
# .two
