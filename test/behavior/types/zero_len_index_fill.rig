# An index into a `[0 of x]` fill panics when the program runs.

fun at(i: Int) -> Int
  [0 of 7][i]

sub main()
  i = 0
  for j in 0..1
    i += j
  print(at(i))

# expect-panic: index out of bounds
