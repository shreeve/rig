# Arithmetic on a `pre` parameter runs when the program runs: an overflow
# panics instead of failing to compile.

sub show(pre n: U8)
  print(n + 250)

sub main()
  show(1)
  show(10)

# expect-panic: integer overflow

# expect:
# 251
