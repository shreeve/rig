# Unannotated mutable bindings (`sum = 0` then `sum += i`) infer Int.

fun sum_to(n: Int) -> Int
  sum = 0
  i = 1
  while i <= n
    sum += i
    i += 1
  sum

sub main()
  print(sum_to(10))

# expect:
# 55
