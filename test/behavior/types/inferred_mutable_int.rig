# M19: a mutable accumulator with no source-level type annotation.
# Sema infers `Int` from the literal RHS; the emitter renders it as
# `var sum: i32 = 0;`.
fun sum_to(n: Int) -> Int
  sum = 0
  i = 1
  while i <= n
    sum += i
    i += 1
  sum

sub main()
  print(sum_to(10))
