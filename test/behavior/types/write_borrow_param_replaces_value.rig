# Assigning a `!T` parameter replaces the caller's value; the old value is
# dropped first.

struct Wrap
  v: Int

  drop(!self)
    print(self.v)

sub replace(b: !Wrap)
  b = Wrap(v: 2)

sub main()
  x = Wrap(v: 1)
  replace(!x)
  print(x.v * 10)

# expect:
# 1
# 20
# 2
