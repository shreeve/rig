# A generic struct instantiated as `Wrap[Int]`; the constructor's field is
# checked against the substituted type and `b.value` types as Int.

struct Wrap[T]
  value: T

sub main()
  b: Wrap[Int] = Wrap(value: 5)
  print(b.value + 1)

# expect:
# 6
