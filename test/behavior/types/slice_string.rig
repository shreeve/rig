# `s[a..b]` of a String is a String: the bytes from `a` up to `b`.

fun middle(s: String) -> String
  s[1..s.len - 1]

sub main()
  s = "hello, world"
  hello = s[0..5]
  print(hello, hello.len)
  print(middle("[rig]"))
  empty = s[3..3]
  print(empty.len, empty == "")
  print(s[7..12][0..3], s[7..12][1])
  i = 2
  print(s[i..i + 3])

# expect:
# hello 5
# rig
# 0 true
# wor 111
# llo
