# `while e as x` repeats while `e` yields a value, binding it to `x` each
# time: here until the Vec is empty.

sub main()
  v: Vec[Int] = Vec()
  !v.push(1)
  !v.push(2)
  !v.push(3)
  total = 0
  while !v.pop() as x
    total += x
  print(total)

# expect:
# 6
