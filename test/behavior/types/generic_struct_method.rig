# A method on a generic struct: `get` returns `T`, which is Int for a
# `Wrap[Int]` receiver.

struct Wrap[T]
  value: T

  fun get(?self) -> T
    self.value

sub main()
  b: Wrap[Int] = Wrap(value: 42)
  print(b.get())

# expect:
# 42
