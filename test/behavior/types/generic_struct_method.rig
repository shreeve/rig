# A method on a generic struct: `get` returns `T`, which is Int for a
# `Box(Int)` receiver.

type Box(T)
  value: T

  fun get(?self) -> T
    self.value

sub main()
  b: Box(Int) = Box(value: 42)
  print(b.get())

# expect:
# 42
