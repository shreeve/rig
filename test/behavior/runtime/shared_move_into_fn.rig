# `use_rc(<rc)` moves a shared handle into a function, which owns and
# drops it; the caller does not drop it again.

struct Wrap
  payload: Int

sub use_rc(rc: *Wrap)
  print(rc.payload)
  -rc

sub main()
  rc = *Wrap(payload: 42)
  use_rc(<rc)
  print "moved"

# expect:
# 42
# moved
