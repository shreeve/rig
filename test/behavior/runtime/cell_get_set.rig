# `Cell.get` and `Cell.set` on a stack-local cell: interior mutation
# through a binding that is never reassigned.

sub main()
  c: Cell[Int] = Cell(value: 0)
  c.set(5)
  print(c.get())
  c.set(c.get() + 10)
  print(c.get())

# expect:
# 5
# 15
