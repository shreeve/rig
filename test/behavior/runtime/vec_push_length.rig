# `Vec(Int)`: push three values and read the length; the buffer is
# freed at scope exit.

sub main()
  v: Vec(Int) = Vec()
  (!v).push(10)
  (!v).push(20)
  (!v).push(30)
  print(v.length())                 # 3

# expect:
# 3
