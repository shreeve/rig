# `*Vec(Int)`: a Vec inside a shared box; pushes go through the handle
# and the Vec is freed when the last strong handle drops.

sub main()
  rv: *Vec(Int) = *Vec()
  (!rv).push(100)
  (!rv).push(200)
  print(rv.length())                # 2

# expect:
# 2
