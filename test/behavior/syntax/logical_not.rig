# `not a` is logical negation of a Bool.
# Known bug (A5): the grammar has no production for `not`, so `not` is
# parsed as an identifier and check reports "use of unbound name `not`".

sub main()
  a = false
  if not a
    print(1)
  b = not a
  print(b)

# expect:
# 1
# true
