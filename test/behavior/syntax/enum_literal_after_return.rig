# `return .red` returns the enum literal `.red` of the declared return
# type, and a match covering every variant needs no default arm.

enum Color
  red
  green

fun pick(b: Bool) -> Color
  if b
    return .red
  return .green

sub main()
  match pick(true)
    .red => print(1)
    .green => print(2)
  match pick(false)
    .red => print(1)
    .green => print(2)

# expect:
# 1
# 2
