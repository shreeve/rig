examples/closure_resource_default_rejected.rig:14:8: error: bare capture `|rc|` of shared handle `*T` would hide a refcount bump; use `|+rc|` to clone, `|<rc|` to move, or `|~rc|` to capture a weak ref
