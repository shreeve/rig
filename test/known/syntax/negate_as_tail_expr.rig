# `-x` as the last line of a `fun` is its value: numeric negation.
# Known bug (A4): the rewriter classifies a statement-start `-x` as a drop,
# so the function has no return value and the emitted Zig fails to
# compile.

fun neg(x: Int) -> Int
  -x

sub main()
  print(neg(3))

# expect:
# -3
