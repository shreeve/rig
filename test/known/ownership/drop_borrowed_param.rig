# Known bug (C7): `-rc` on a read-borrowed parameter is accepted; the
# caller still owns the handle. Today `rig check` accepts this.

struct Box
  payload: Int

sub kill(rc: ?*Box)
  -rc

sub main()
  rc = *Box(payload: 5)
  kill(?rc)
  print(rc.payload)

# error: cannot drop borrowed
