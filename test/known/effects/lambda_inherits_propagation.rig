# Known bug (B4): a lambda body inherits the enclosing function's
# permission to use `!`, but the closure returns Void and can't
# propagate. The fallible `g` body also hits B1.

fun g() -> Int!
  3

sub main()
  n = 1
  c = |n|
    print(g()! + n)
  c()

# error: use of `!` propagation requires
