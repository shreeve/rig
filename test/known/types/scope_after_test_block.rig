# Known bug (B9): sema replays scopes by a shared counter; a `test` block
# before a function desynchronizes it, so the valid locals in `main` are
# reported as unbound.

test "t"
  a = 1
  print(a)

sub main()
  x = "hi"
  print(x)

# expect:
# hi
