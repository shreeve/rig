# Known bug (B6): `type UserId = Int` resolves to an opaque nominal, so an
# Int literal can't initialize a `UserId`; an alias should be the same type.

type UserId = Int

fun next(a: UserId) -> Int
  a + 1

sub main()
  x: UserId = 5
  print(next(x))

# expect:
# 6
