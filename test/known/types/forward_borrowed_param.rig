# Known bug (B11): a read-borrowed parameter can't be forwarded; `g(?b)`
# with `b: ?B` types as `??B` and is rejected.

struct B
  n: Int

fun g(b: ?B) -> Int
  b.n

fun f(b: ?B) -> Int
  g(?b)

sub main()
  x = B(n: 1)
  print(f(?x))

# expect:
# 1
