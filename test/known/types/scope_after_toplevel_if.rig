# Known bug (B9): a top-level `if` before a function desynchronizes sema's
# scope replay, so the valid local `n` in `main` is reported as unbound.

k = if true
  1
else
  2

sub main()
  n = 5
  print(n)

# expect:
# 5
