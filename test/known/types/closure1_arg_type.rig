# Known bug (B6): invoking a `*Closure1(Int)` with a String is accepted;
# the argument check is left to Zig.

sub main()
  v: *Cell(Int) = *Cell(value: 0)
  cb: *Closure1(Int) = *Closure1(Int)(|+v| (a: Int) v.set(a))
  cb("seven")
  print(v.get())

# error: expected `Int`, got `String`
