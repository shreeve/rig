# `a..b` is a half-open integer range; `for i in a..b` iterates it.
# Sema types it (the binding is `Int`). Known bug (D17, emit): emit
# writes a @compileError for the range.

sub main()
  total = 0
  for i in 0..4
    total += i
  print(total)

# expect:
# 6
