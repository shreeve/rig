# `a..b` is a half-open integer range; `for i in a..b` iterates it.
# Known bug (D17): emit writes a @compileError for ranges in expression
# position.

sub main()
  total = 0
  for i in 0..4
    total += i
  print(total)

# expect:
# 6
