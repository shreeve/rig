# Known bug (B6): `self.value + 1` in a generic body is accepted when the
# type is instantiated with T = String; only Zig rejects the `+`.

type Box(T)
  value: T

  fun bump(?self) -> T
    self.value + 1

sub main()
  b: Box(String) = Box(value: "a")
  print(b.bump())

# error: `String`
