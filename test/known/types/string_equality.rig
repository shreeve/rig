# `==` on two Strings compares their contents.
# Known bug (B5, emit): lowered to Zig `==` on slices, which Zig rejects
# ("cannot compare strings with =="); it needs `std.mem.eql`.

sub main()
  s = "a"
  print(s == "a")
  print(s == "b")

# expect:
# true
# false
