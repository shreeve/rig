# Known bug (B5): `==` on two Strings is accepted but lowered to Zig `==`
# on slices, which Zig rejects ("cannot compare strings with ==").

sub main()
  s = "a"
  print(s == "a")
  print(s == "b")

# expect:
# true
# false
