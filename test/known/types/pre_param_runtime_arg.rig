# Known bug (B6): a runtime value passed to a `pre` (compile-time)
# parameter is accepted; only Zig rejects it ("unable to resolve comptime
# value").

sub show(pre n: Int)
  print(n)

sub main()
  k = 0
  k += 3
  show(k)

# error: `pre`
