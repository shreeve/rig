# Known bug (B12): a `-> Int` function whose last statement is a `while`
# produces no value but is accepted; emit writes a @compileError.

fun f() -> Int
  while false
    print(1)

sub main()
  print(f())

# error: `Int`
