# `v[i]` reads an element of a Vec of Copy values (bounds-checked).
# Sema types it as the element type. Known bug (D17, emit): the emitted
# Zig indexes the Vec struct itself and fails to compile.

sub main()
  v: Vec(Int) = Vec()
  (!v).push(4)
  (!v).push(9)
  print(v[1])

# expect:
# 9
