# `v[i]` reads an element of a Vec of Copy values (bounds-checked).
# Known bug (D17): sema accepts indexing a Vec but the emitted Zig fails
# to compile.

sub main()
  v: Vec(Int) = Vec()
  (!v).push(4)
  (!v).push(9)
  print(v[1])

# expect:
# 9
