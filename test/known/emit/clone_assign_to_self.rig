# Known bug (D5): `a = +a` drops the old handle before evaluating the
# right-hand side, so the clone reads a freed box; today this segfaults.

struct Box
  n: Int

sub main()
  a = *Box(n: 3)
  a = +a
  print(a.n)

# expect:
# 3
