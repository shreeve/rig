# Known bug (D11): a closure that move-captures a `Vec` never drops it
# (capture cleanup only knows shared and weak handles), so the Vec's
# buffer leaks.

sub main()
  v: Vec(Int) = Vec()
  (!v).push(1)
  (!v).push(2)
  f = |<v|
    print(v.length())
  f()

# expect:
# 2
