# Known bug (D8): the `else` block of a `while` (run when the loop ends
# without `break`) is dropped by the emitter.

sub main()
  i = 0
  while i < 2
    i += 1
  else
    print("done")
  print(i)

# expect:
# done
# 2
