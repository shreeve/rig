# Known bug (B7, D7): a field assignment `u.name = "y"` is dropped by emit,
# so the old value prints.

struct User
  name: String

sub main()
  u = User(name: "x")
  u.name = "y"
  print(u.name)

# expect:
# y
