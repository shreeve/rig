# Known bug (new, emit): a `raw` block used as a function's value is
# accepted, but emit writes a @compileError for `raw_block`.

extern fun abs(n: Int) -> Int

fun safe_abs(n: Int) -> Int
  raw
    abs(n)

sub main()
  print(safe_abs(-5))

# expect:
# 5
