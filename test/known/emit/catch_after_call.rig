# `f() catch 0` recovers from a failure of `f()` with 0; here `may`
# succeeds, so the result is its value.
# Known bug: emit writes a discarded error capture Zig rejects

fun may() -> Int!
  return 42

sub main()
  y = may() catch 0
  print(y)

# expect:
# 42
