# Known bug: a parameter the body never reads is emitted as-is, and Zig
# rejects the program with "unused function parameter".

sub greet(name: String)
  print "hello"

sub main()
  greet("ada")

# expect:
# hello
