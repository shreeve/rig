# Known bug (D8): the `: i += 1` continuation of a `while` is dropped by
# the emitter, so `i` is never incremented (the Zig fails to compile with
# "local variable is never mutated"; otherwise the loop would not end).

sub main()
  i = 0
  while i < 3 : i += 1
    print(i)

# expect:
# 0
# 1
# 2
