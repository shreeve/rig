# Known bug (B12, D17): keyword arguments to a plain function are accepted
# by sema, but emit writes a @compileError for each `kwarg`.

fun sub_ab(a: Int, b: Int) -> Int
  a - b

sub main()
  print(sub_ab(b: 1, a: 5))

# expect:
# 4
