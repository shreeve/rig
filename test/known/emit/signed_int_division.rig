# Known bug: `/` on signed Ints is emitted as Zig `/`, which Zig rejects
# for signed integers ("must use @divTrunc, @divFloor, or @divExact").

fun half(n: Int) -> Int
  n / 2

sub main()
  print(half(7))
  print(half(-7))

# expect:
# 3
# -3
