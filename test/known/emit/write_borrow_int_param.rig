# Known bug (D7): a `c: !Int` parameter is lowered as a by-value `i32`, so
# assigning through it fails to compile ("cannot assign to constant").

sub bump(c: !Int)
  c = c + 1

sub main()
  n = 1
  bump(!n)
  print(n)

# expect:
# 2
