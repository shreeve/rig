# Known bug (D12): an owned value parameter (`v: Vec(Int)`) is emitted as
# a Zig const parameter, so dropping it fails to compile (`*const Vec`
# where `*Vec` is needed).

sub eat(v: Vec(Int))
  print(v.length())

sub main()
  v: Vec(Int) = Vec()
  (!v).push(1)
  eat(<v)

# expect:
# 1
