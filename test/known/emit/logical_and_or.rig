# `&&` and `||` combine Bools.
# Known bug (new, emit): emitted verbatim as Zig `&&` / `||`, which Zig
# rejects ("use 'and' for logical AND").

sub main()
  a = 7 > 3 && 2 < 5
  b = 1 > 2 || 3 > 4
  print(a)
  print(b)

# expect:
# true
# false
