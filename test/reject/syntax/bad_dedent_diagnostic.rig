# Dedenting to a column that matches no enclosing block is an indentation
# error reported at the offending line.

sub main()
    x = 1
  print(x)

# error: 6:3: indentation does not match any enclosing block
