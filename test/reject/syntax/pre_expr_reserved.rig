## M22.1(4/8): `pre <expr>` expression-modifier form is reserved in V1.
##
## Per GPT-5.5 entry 39: parallel to `pre_block`, the expression
## form `x = pre 42` parsed cleanly but emit produced
## `@compileError`. Reserved in V1; `pre_param` is the supported
## compile-time surface.

sub main()
  x = pre 42           # error: `pre` expression reserved
  print(x)
