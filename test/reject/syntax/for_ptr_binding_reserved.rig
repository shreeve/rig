# `for *x in v` (by-reference loop binding) is reserved and does not
# parse; iterate with `for x in v`, `?v`, or `!v`.

sub main()
  v: Vec[Int] = Vec()
  !v.push(1)
  for *x in v
    print(x)

# error: `for *x in` is reserved
