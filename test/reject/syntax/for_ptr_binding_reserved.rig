# `for *x in v` (by-reference loop binding) is reserved; iterate Copy
# elements with `for x in v`.

sub main()
  v: Vec(Int) = Vec()
  (!v).push(1)
  for *x in v
    print(x)

# error: by-reference for-loop binding `for *x in ...` is reserved
