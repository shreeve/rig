# In a size, as anywhere, an operator touching only what follows it is a
# prefix: `[N -1]` is not `[N - 1]`.

N =! 4

sub main()
  a: [N -1]Int = [3 of 0]
  print(a)

# error: 7:9: unexpected `-`; `-` touching `1` is a prefix (the spacing rule); for arithmetic, write `N - 1`
