# Keywords are reserved outside member names: `in:` inside ( ) reads as
# a name, but `in` cannot name a parameter.

sub show(in: Int)
  print(1)

sub main()
  show(1)

# error: 4:10: `in` is a keyword and cannot name a parameter
