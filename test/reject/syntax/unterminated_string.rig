# A string literal with no closing quote is reported as unterminated at
# its line and column.
# Known bug (A9): check prints "parse error in `<file>`" with no position,
# and the underlying parser message is "unexpected err".

sub main()
  x = "abc
  print(x)

# error: unterminated string
