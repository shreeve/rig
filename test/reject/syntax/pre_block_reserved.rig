# A `pre` block is reserved and does not parse; only `pre` function
# parameters are compile-time forms.

sub main()
  pre
    x = 42
    print(x)

# error: unexpected keyword `pre`
# error: `pre` marks compile-time parameters only
