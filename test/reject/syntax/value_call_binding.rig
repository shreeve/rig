# A paren-free call is a command; the right side of a binding is a
# value, so a call there takes parentheses.

fun twice(n: Int) -> Int
  n * 2

sub main()
  x = twice 5
  print(x)

# error: 8:13: unexpected `5`; a call where a value is expected takes parentheses: `twice(5)`
