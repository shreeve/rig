# `a +b` in a value position: add, or call `a` with `b` cloned.

sub main()
  a = 2
  b = 3
  c = a +b
  print(c)

# error: 6:9: unexpected `+`; a sigil touching its operand is a prefix: to add, write `a + b`; to call `a`, write `a(+b)`
