# A `drop` declaration takes exactly one parameter, its receiver.

struct File
  fd: Int

  drop(!self, extra: Int)
    print(self.fd + extra)

# error: `drop` takes exactly one parameter, its receiver: `drop(!self)`; got 2
