# A generic body cannot call methods on a type parameter.

struct Wrap[T]
  value: T

  fun show(?self) -> Int
    self.value.len()

sub main()
  print(1)

# error: a generic parameter `T` has no methods
