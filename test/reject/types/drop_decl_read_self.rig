# A `drop` declaration's receiver is write-borrowed, `drop(!self)`; a
# read borrow is rejected.

struct File
  fd: Int

  drop(?self)
    print(self.fd)

# error: `drop` takes its receiver write-borrowed: `drop(!self)`
