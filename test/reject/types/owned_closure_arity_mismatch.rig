# Calling a zero-argument `*Closure()` with arguments is an arity error.

sub main()
  count: *Cell(Int) = *Cell(value: 0)
  cb: *Closure() = *Closure(|+count| count.set(count.get() + 1))
  cb(1, 2)

# error: owned closure `cb` invocation expects 0 argument(s); got 2
