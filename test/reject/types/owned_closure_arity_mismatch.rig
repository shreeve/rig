# Calling a zero-argument `*sub()` with arguments is an arity error.

sub main()
  count: *Cell[Int] = *Cell(value: 0)
  cb: *sub() = *|+count| count.set(count.get() + 1)
  cb(1, 2)

# error: call to `cb` expects 0 arguments, got 2
