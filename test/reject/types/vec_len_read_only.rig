# A Vec's `.len` is read-only; elements are added with `push` and
# removed with `pop` or `clear`.

sub main()
  v: Vec(Int) = Vec()
  (!v).len = 0
  print(v.len)

# error: 6:8: cannot assign to `.len`; a length is read-only
