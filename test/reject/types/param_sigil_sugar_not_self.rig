# Sigil-on-name parameter sugar is only for `self`; other parameters put
# the sigil on the type (`xs: ?Type`).

fun first(?xs) -> Int
  42

sub main()
  print(first(1))

# error: sigil-prefixed parameter is only allowed for `self`
