# An argument for a compile-time parameter must be known at compile
# time.

sub show[n: Int]
  print(n)

sub main()
  k = 0
  k += 3
  show[k]()

# error: 10:8: compile-time argument 1 of `show` must be known at compile time; pass a literal, an enum value, a module constant, a compile-time parameter, or a `=!` binding of one
