# `!` needs a fallible operand; applying it to a call that cannot fail is
# rejected.

fun one() -> Int
  1

sub main()
  n = one()!
  print(n)

# error: 8:7: `!` needs a fallible operand; this expression has type `Int` and cannot fail
