# A generic constructor's fields are checked against the substituted
# type from the expected `Box(Int)`.

type Box(T)
  value: T

sub main()
  b: Box(Int) = Box(value: "hello")
  print(b.value)

# error: type mismatch: expected `Int`, got `String`
