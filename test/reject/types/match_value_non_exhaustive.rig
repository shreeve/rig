# A value-position match must cover every variant or have a default arm.

enum Color
  red
  green
  blue

sub main()
  c: Color = .red
  x = match c
    .red => 1
    .green => 2

# error: value-position `match` is not exhaustive
