# An argument for a `pre` (compile-time) parameter must be known at
# compile time.

sub show(pre n: Int)
  print(n)

sub main()
  k = 0
  k += 3
  show(k)

# error: 10:8: argument 1 of `show` is a `pre` parameter and must be known at compile time; pass a literal, an enum value, a `pre` parameter, or a `=!` binding of one
