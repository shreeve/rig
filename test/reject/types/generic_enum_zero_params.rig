# `enum Foo()` declares a generic with no type parameters, which is
# rejected; a plain enum drops the `()`.

enum Foo()
  bar
  baz

sub main()
  print("done")

# error: generic enum `Foo` must declare at least one type parameter
