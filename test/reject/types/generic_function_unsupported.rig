# `pre T: type` would declare a generic function, which Rig does not
# support yet; the function is reported once, not at each use of `T`.

fun size(pre T: type, x: T) -> Int
  y: T = x
  3

sub main()
  print(1)

# error: generic functions are not supported yet: `pre T: type` would make `size` generic
