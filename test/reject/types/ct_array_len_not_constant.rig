# An array's length is a compile-time integer: not a type, a run-time
# value, a float, or arithmetic on a compile-time parameter, which Rig
# cannot check for overflow at each instance.

HALF =! 2.5

fun grow[n: Int](xs: [n + 1]Int) -> Int
  1

sub main()
  k = 3
  a: [k]Int = [1, 2, 3]
  b: [Int]Int = [1]
  c: [HALF]Int = [1, 2]
  d: [4 / 0]Int = [1]
  e: [nope]Int = [1]
  print(a, b, c, d, e)

# error: 7:23: an array length `n + 1` does arithmetic on a compile-time parameter
# error: 12:7: an array length must be known at compile time; `k` is not
# error: 13:7: `Int` is a type; an array length is an integer, a constant, or a compile-time parameter
# error: 14:7: an array length must be known at compile time; `HALF` is not
# error: 15:7: an array length `4 / 0` divides by zero
# error: 16:7: use of unbound name `nope`
