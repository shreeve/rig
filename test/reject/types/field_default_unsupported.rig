# Field default values are not supported yet; the backend would drop the
# default, so every constructor passes every field.

struct Config
  retries: Int = 3

sub main()
  c = Config(retries: 1)
  print(c.retries)

# error: field default values are not supported yet
