# An owned closure must be constructed with `*Closure(...)`; bare
# `Closure(...)` is rejected.

sub main()
  count: *Cell(Int) = *Cell(value: 0)
  cb = Closure(|+count| count.set(count.get() + 1))

# error: owned closure must be wrapped with `*`
