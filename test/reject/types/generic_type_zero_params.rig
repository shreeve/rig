# A `type` with members is generic: it declares its type parameters in
# brackets. One without them is a `struct`.

type Foo
  bar: Int

sub main()
  print("done")

# error: generic type `Foo` must declare at least one type parameter (`type Foo[T]`); a type without them is a `struct`
