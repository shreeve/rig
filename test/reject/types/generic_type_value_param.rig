# A generic type's compile-time value parameter is an integer.

enum Mode
  fast
  slow

struct Tuned[m: Mode]
  first: Int

sub main()
  print(1)

# error: 7:17: a compile-time value parameter of a type is an integer; `m` has the type `Mode`
