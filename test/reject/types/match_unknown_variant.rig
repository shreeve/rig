# A match arm pattern must name a variant of the scrutinee's enum.

enum Color
  red
  green

sub main()
  c: Color = .red
  match c
    .red => print "R"
    .purple => print "P"

# error: no variant `purple` on enum `Color`
