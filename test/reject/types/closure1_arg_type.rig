# Invoking a `*sub(Int)` checks the argument against `Int`.

sub main()
  v: *Cell(Int) = *Cell(value: 0)
  cb: *sub(Int) = *|+v, a: Int| v.set(a)
  cb("seven")
  print(v.get())

# error: expected `Int`, got `String`
