# A generic type in type position needs its type arguments: `Box`
# alone is not a type, `Box[Int]` is.

struct Box[T]
  value: T

sub main()
  x: Box = Box(value: 5)
  print(x.value)

# error: generic type `Box` requires type arguments
