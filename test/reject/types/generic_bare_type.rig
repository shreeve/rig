# A generic type in type position needs its type arguments: `Wrap`
# alone is not a type, `Wrap[Int]` is.

struct Wrap[T]
  value: T

sub main()
  x: Wrap = Wrap(value: 5)
  print(x.value)

# error: generic type `Wrap` requires type arguments
