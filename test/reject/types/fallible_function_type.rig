# A function type cannot be fallible: only a declared function's return
# type can.

struct Job
  run: fun() Int!

fun apply(f: fun(Int) Int!, n: Int) -> Int!
  f(n)!

sub main()
  print(1)

# error: a fallible type `Int!` is only allowed as a function's return type
