# A `for` source must be iterable; an Int is not.

sub main()
  x = 3
  for i in x
    print(i)

# error: 5:12: cannot iterate over `Int`; a `for` source must be a range `a..b`, an array, a String, or a `Vec`
