# `Vec(T)` rejects a plain struct element type.

struct User
  name: String

sub takeVec(v: Vec(User))
  print("placeholder")

sub main()
  print("hi")

# error: requires `T` to be a Copy type (Int, Bool, Float, String), a shared handle
