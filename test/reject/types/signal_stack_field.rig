# A struct holds a Signal through a shared handle (`s: *Signal(Int)`).

struct H
  s: Signal(Int)

sub main()
  h = H(s: Signal(value: 1))
  print(h.s.get())

# error: stack-local `Signal(T)` is not supported
