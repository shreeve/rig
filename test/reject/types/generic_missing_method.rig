# Calling a method that doesn't exist on a generic receiver names the
# generic type.

struct Wrap[T]
  value: T

sub main()
  b: Wrap[Int] = Wrap(value: 5)
  print(b.missing())

# error: no method `missing` on type `Wrap`
