# A type alias is transparent (`N` is `Int`), so it is not a constructor;
# `N(5)` is rejected.

type N = Int

sub main()
  x = N(5)
  print(x)

# error: 7:7: `N` is a type alias for `Int` and cannot be called as a constructor; construct the aliased type directly
