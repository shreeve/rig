# Field access on an optional (`u.name` with `u: User?`) requires
# unwrapping it first.

struct User
  name: String

fun name_of(u: User?) -> String
  u.name

sub main()
  print("unused")

# error: 8:5: cannot access `name` on optional `User?`; take the value out first with `x ?? fallback`
