# A generic constructor needs an expected type to fix `T`; there is no
# generic inference.

type Box(T)
  value: T

sub main()
  b = Box(value: 5)
  print(b.value)

# error: generic constructor `Box` requires an expected type
