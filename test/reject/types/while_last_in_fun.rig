# A `-> Int` function must end with a value; a trailing `while` produces
# none.

fun f() -> Int
  while false
    print(1)

sub main()
  print(f())

# error: 5:3: a function returning `Int` must end with a value; this `while` produces none
