# Calling a `*sub(Int)` with zero or two arguments is an arity error.

sub main()
  v: *Cell(Int) = *Cell(value: 0)
  cb: *sub(Int) = *|+v, a| v.set(a)
  cb()
  cb(1, 2)

# error: call to `cb` expects 1 argument, got 0
# error: call to `cb` expects 1 argument, got 2
