# A `sub` produces no value, so binding its result is rejected.

sub g()
  print(1)

sub main()
  x = g()
  y = x + 1
  print(y)

# error: 7:7: `x` would be bound to a value of type `Void`; this expression produces no value
