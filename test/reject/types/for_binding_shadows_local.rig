# A `for` binding may not reuse the name of a visible local (implicit
# shadowing is illegal).

sub main()
  n = 99
  v: Vec[Int] = Vec()
  !v.push(1)
  for n in v
    print(n)
  print(n)

# error: 8:7: `for` binding `n` shadows the local `n`; use a different name
