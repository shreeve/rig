# An extern function can only be called, inside `raw`: binding it to a
# name would let it be called later without `raw`.

extern fun abs(n: Int) -> Int

sub main()
  raw
    f: fun(Int) Int = abs
    print(f(-1))

# error: an extern function can only be called, inside `raw`
