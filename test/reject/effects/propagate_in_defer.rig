# A deferred expression cannot propagate with `!`.

fun g() -> Int!
  3

sub main()
  defer print(g()!)
  print(1)

# error: 7:15: cannot use `!` inside `defer`; a deferred expression cannot propagate failure, so handle it with `catch`
