# A closure body cannot return `none` from the function around it.

fun f(m: Int?) -> Int?
  g = |+m| m? + 1
  g()

sub main()
  print(f(1))

# error: 4:12: a closure body cannot return `none` with `?`
