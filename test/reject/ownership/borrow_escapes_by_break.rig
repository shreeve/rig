# `break` leaves the loop body's scope: a borrow of one of its locals
# cannot be carried out.

struct Box
  payload: Int

sub main()
  a = Box(payload: 1)
  r = ?a
  while true
    b = Box(payload: 2)
    r = ?b
    break
  print(r.payload)

# error: `b` does not live long enough
