# A closure that captured a borrow keeps the owner borrowed while the
# closure binding is live.

struct Wrap
  payload: Int

  drop(!self)
    print("drop")

sub main()
  a = Wrap(payload: 1)
  r = ?a
  f = |<r|
    print(r.payload)
  -a
  f()

# error: cannot drop `a` while borrows are live
