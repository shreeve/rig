# A bare `|rc|` capture of a shared handle in a local lambda is
# rejected: it would copy the handle without a refcount bump.

sub main()
  rc: *Cell(Int) = *Cell(value: 0)
  f = |rc|
    rc.get()
  print(f())

# error: bare capture `|rc|` of shared handle
