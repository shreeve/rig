# A borrow stored in a struct field keeps its owner borrowed while the
# struct is live.

struct Wrap
  payload: Int

struct View
  box: ?Wrap

sub main()
  b = Wrap(payload: 5)
  v = View(box: ?b)
  -b
  print(v.box.payload)

# error: cannot drop `b` while borrows are live
