# Moving a captured resource out is rejected in an arity-bearing owned
# closure too.

sub takes_owned(c: *Cell(Int))
  c.set(0)

sub main()
  sig: *Cell(Int) = *Cell(value: 0)
  bad: *Closure1(Int) = *Closure1(Int)(|+sig| (a: Int) takes_owned(<sig))

# error: cannot move captured resource `sig`
