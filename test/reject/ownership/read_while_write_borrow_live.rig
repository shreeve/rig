# Reading a value while a write borrow of it is live is rejected.

struct User
  name: String

sub main()
  user = User(name: "Steve")
  w = !user
  print user

# error: use of `user` while a write borrow is live
