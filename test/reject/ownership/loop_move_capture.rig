# A `|<rc|` move-capture inside a loop is seen by the next iteration.

struct Box
  payload: Int

sub main()
  rc = *Box(payload: 1)
  i = 0
  while i < 2
    f = |<rc|
      print(rc.payload)
    f()
    i += 1

# error: cannot capture `rc` after move
