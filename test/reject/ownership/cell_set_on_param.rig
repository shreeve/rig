# `Cell.set` needs an addressable receiver; a by-value Cell parameter is
# not one.

sub bump(c: Cell(Int))
  c.set(1)

sub main()
  c: Cell(Int) = Cell(value: 0)
  bump(c)

# error: `Cell.set` requires an addressable Cell receiver
