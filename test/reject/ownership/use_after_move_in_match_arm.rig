# A move in one match arm makes the value unusable after the match.

struct Wrap
  payload: Int

sub eat(rc: *Wrap)
  print(rc.payload)

sub main()
  rc = *Wrap(payload: 5)
  n = 1
  match n
    1 => eat(<rc)
    _ => print(0)
  print(rc.payload)

# error: use of `rc` after move
