# A local closure cannot move-capture a borrowed parameter; the caller
# still owns it.

struct Wrap
  payload: Int

sub f(rc: ?*Wrap)
  g = |<rc|
    print(rc.payload)
  g()

sub main()
  rc = *Wrap(payload: 5)
  f(?rc)
  print(rc.payload)

# error: cannot move-capture borrowed
