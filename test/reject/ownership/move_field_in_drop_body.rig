# A drop body cannot move a resource field out: fields are dropped
# automatically after the body returns.

struct Owner
  cell: *Cell[Int]

  drop(!self)
    stash = <self.cell

# error: cannot move out of `self.cell`: `self` is borrowed
