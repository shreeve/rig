# A by-value (consuming) method cannot be called through a shared
# handle.

struct User
  name: String

  sub consume(self: User)
    print(self.name)

sub main()
  rc = *User(name: "x")
  rc.consume()
  -rc

# error: method `consume` consumes the receiver
