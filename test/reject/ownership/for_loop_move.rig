# A move inside a `for` body is seen by the next iteration.

struct Wrap
  payload: Int

sub eat(rc: *Wrap)
  print(rc.payload)

sub main()
  rc = *Wrap(payload: 1)
  for i in 0..3
    eat(<rc)

# error: use of `rc` after move
