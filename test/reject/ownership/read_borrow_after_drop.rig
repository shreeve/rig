# SPEC §"Use After Drop" — must be rejected.
#
# `-user` ends ownership immediately. Subsequent use is invalid.

struct User
  name: String

sub main()
  user = User(name: "Steve")
  -user
  print ?user
