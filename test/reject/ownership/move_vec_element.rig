# An element cannot be moved out of a Vec: the Vec still owns and drops
# the slot.

sub eat(c: *Cell(Int))
  print(c.get())

sub main()
  c1: *Cell(Int) = *Cell(value: 7)
  v: Vec(*Cell(Int)) = Vec()
  (!v).push(<c1)
  eat(<v[0])

# error: cannot move out of
