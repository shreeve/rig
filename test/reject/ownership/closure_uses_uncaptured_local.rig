# A closure body can only use outer locals it captures.

struct Wrap
  payload: Int

sub eat(rc: *Wrap)
  print(rc.payload)

sub main()
  rc = *Wrap(payload: 1)
  n = 5
  f = |+n|
    eat(<rc)
  f()

# error: `rc` is a local of the enclosing function; capture it
