# A shared handle chosen by an if-expression is still a bare copy of
# the handle: each branch must write `<rc` or `+rc`.

struct Box
  payload: Int

sub main()
  rc = *Box(payload: 7)
  x = if true
    rc
  else
    rc
  print(x.payload)

# error: would alias the handle
