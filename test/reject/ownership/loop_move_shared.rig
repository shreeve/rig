# A move inside a `while` body is seen by the next iteration.

struct Wrap
  payload: Int

sub eat(rc: *Wrap)
  print(rc.payload)

sub main()
  rc = *Wrap(payload: 42)
  i = 0
  while i < 3
    eat(<rc)
    i += 1

# error: use of `rc` after move
