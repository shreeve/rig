# A write-receiver method call needs the receiver unborrowed.

struct User
  age: Int

  sub bump(!self, by: Int)
    self.age = self.age + by

sub main()
  u = User(age: 1)
  r = ?u
  !u.bump(1)
  print(r.age)

# error: cannot write-borrow `u` while a read borrow is live
