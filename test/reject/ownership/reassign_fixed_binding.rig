# A binding made with `=!` is fixed and cannot be reassigned.

struct User
  name: String

fun loadUser(id: Int) -> User
  User(name: "Steve")

sub main()
  user =! loadUser(1)
  user = loadUser(2)

# error: cannot reassign fixed binding `user`
