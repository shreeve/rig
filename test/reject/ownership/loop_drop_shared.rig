# A drop inside a `while` body is seen by the next iteration's read.

struct Wrap
  payload: Int

sub main()
  rc = *Wrap(payload: 42)
  i = 0
  while i < 2
    print(rc.payload)
    -rc
    i += 1

# error: use of `rc` after drop
