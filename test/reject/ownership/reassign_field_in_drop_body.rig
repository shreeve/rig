# A drop body cannot reassign a resource field: fields are dropped
# automatically after the body returns.

struct Owner
  cell: *Cell(Int)

  drop self: !Owner
    fresh: *Cell(Int) = *Cell(value: 0)
    self.cell = <fresh

# error: cannot reassign resource field `self.cell` inside drop body
