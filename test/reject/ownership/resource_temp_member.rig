# Reading a field of a fresh `*User(...)` without binding it first is
# rejected: the anonymous handle would leak.

struct User
  age: Int

sub main()
  a = (*User(age: 5)).age
  print(a)

# error: resource allocation `*User` used as an anonymous temporary
