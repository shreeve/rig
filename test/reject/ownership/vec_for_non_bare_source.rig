# Iterating a Vec of resources needs a local Vec binding as the source,
# not a call result.

fun makeSubs() -> Vec(*sub())
  v: Vec(*sub()) = Vec()
  <v

sub main()
  for cb in ?makeSubs()
    cb()

# error: requires a bare local Vec binding as the source
