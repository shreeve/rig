# Reading a field of a resource-returning call without binding it first
# is rejected: the anonymous handle would leak.

struct User
  age: Int

fun make_user() -> *User
  return *User(age: 5)

sub main()
  a = make_user().age
  print(a)

# error: resource-valued call result used as an anonymous temporary
