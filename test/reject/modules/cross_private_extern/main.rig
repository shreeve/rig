# `extern` does not bypass `pub`: a private extern in module `a` is
# invisible to importers, who must go through the `pub` safe wrapper.
use a

sub main()
  raw
    a.puts("hi")

# error: `a.puts` is not public; mark it `pub` in module `a`
