# An importer cannot name an instance of another module's generic type,
# so a public function may not return one.

use lib

sub main()
  b = lib.make(1)
  print(b.v)

# error: public function `make` exposes `Wrap[Int]`, an instance of a generic type
