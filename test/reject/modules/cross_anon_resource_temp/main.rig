# Using a field of an imported function's `*Box` result without binding
# it would leak the Rc (no auto-drop guard on an anonymous temporary), so
# the anonymous-resource-temporary rule applies across modules too.
use a

sub main()
  print(a.make_box().value)

# error: resource-valued call result used as an anonymous temporary
