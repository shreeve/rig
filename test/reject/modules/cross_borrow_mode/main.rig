# `a.log_box` takes a read borrow `?Wrap`; passing a bare `Wrap` violates
# the imported parameter's borrow mode.
use a

sub main()
  b = a.make_box(42)
  a.log_box(b)

# error: type mismatch: expected `?a.Wrap`, got `a.Wrap`
