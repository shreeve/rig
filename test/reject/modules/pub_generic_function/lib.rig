pub fun same[T](x: T) -> T
  x

pub struct Tagger
  label: String

  sub show[T](?self, x: T)
    print(self.label, x)

fun local[T](x: T) -> T
  x

struct Inner
  n: Int

  fun same[U](?self, x: U) -> U
    x

pub struct Outer
  i: Inner

pub fun uses_local(n: Int) -> Int
  local(n)
