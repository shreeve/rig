# A field of a public struct is reachable from importers, so it may not
# hold an instance of a generic type either.

use lib

sub main()
  h = lib.make()
  print(h.b.v)

# error: public field `b` exposes `Wrap[Int]`, an instance of a generic type
